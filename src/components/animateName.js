import { elClass } from '../utils';

export default function animateName() {
  const nameBold = elClass('div', 'name-bold');
  const nameThin = elClass('div', 'name-thin');
  const namePipes = elClass('div', 'name-pipes');
  const nameO = elClass('div', 'name-o');
  const nameLogo = elClass('div', 'name-logo');
}
